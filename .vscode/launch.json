{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run cmd/server",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/server/main.go"
        }
    ]
}
